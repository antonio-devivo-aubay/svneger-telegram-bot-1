version: '3'

services:
  web:
    build: ./
    restart: unless-stopped
    container_name: docker-bot
    ports:
      - "8443:8443"
    networks:
      - web

networks:
  web:
    name: web
    external: false